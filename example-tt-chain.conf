# local port on your machine
LOCAL_PORT=8080

# remote target port on final machine
REMOTE_PORT=80

# list of hosts in order (no user@ needed unless required)
HOST_CHAIN=(
  host1.example.com
  host2.internal
  targethost.internal
)
